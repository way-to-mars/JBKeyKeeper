<UserControl x:Class="JBKeyKeeper.View.ItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JBKeyKeeper.View"
             xmlns:userControls="clr-namespace:JBKeyKeeper.View"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="500">
    <Border BorderBrush="{StaticResource mainBorderBrush}" BorderThickness="1" VerticalAlignment="Center"
                            CornerRadius="5" Background="{StaticResource mainBackBrush2}" Height="auto"
                            Margin="2,5,5,2" Padding="4"                       
                            >
        <Expander Foreground="{StaticResource fontBrush1}" FontSize="13" Header="Сельпо"
                  x:Name="MainExpander" Expanded="ItemName_Expanded" Collapsed="ItemName_Collapsed">
            <StackPanel Margin="0"
                        x:Name="SubItemsList">
            </StackPanel>
        </Expander>
    </Border>
</UserControl>
